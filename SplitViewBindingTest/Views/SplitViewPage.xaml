<Page
    x:Class="SplitViewBindingTest.Views.SplitViewPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SplitViewBindingTest.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="400"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <StackPanel 
            Grid.Column="0" 
            Orientation="Vertical" 
            VerticalAlignment="Center" HorizontalAlignment="Center">
            <Button
                Height="75"
                Width="200" 
                Command="{x:Bind mySplitVM.HomePageCommand}">
                Home
            </Button>
            <Button
                Height="75"
                Width="200" 
                Margin="0,15,0,0" 
                Command="{x:Bind mySplitVM.OtherPageCommand}">
                Other Page
            </Button>
        </StackPanel>
        <SplitView 
            Grid.Column="1" 
            x:Name="svTransit"
            IsPaneOpen="True"
            DisplayMode="Inline"
            OpenPaneLength="50"
            Content="{x:Bind mySplitVM.PanelPage, Mode=OneWay}">
        </SplitView>

    </Grid>
</Page>
